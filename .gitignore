# SQLite database (per-user, rebuilt from markdown via `bun run ingest`)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# User-pasted images (local only; per-user)
data/user-images/

# Scheduled-run logs
data/logs/

# Node / Bun
node_modules/
.bun/

# Local secrets (SMTP creds, etc)
.env
.env.local

# Throwaway previews/scratch
email-preview.html

# Verify command outputs (generated; per-run)
verify_survivors.md
verify_removed_*.txt

# Accidental path-mangled scratch dumps
UsersJacobMaschler*.html

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Local-only scratch space — drop anything here that shouldn't be pushed
private/
local/
personal/
*.local.md

